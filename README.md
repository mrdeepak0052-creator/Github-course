# GitHub-->
1. For version check
     git -v
2. git initialize
    git init

# Repository-> 
    Repository is a folder or structure.
# Branch->
   Branch is a main project pipline in which we can stay main code and components of project. by default your branch is main.

# for cheching the status of vsc
1. git status
# for checking the branch
  git branch
  git log
 
Main branch
# focheckout and creation the branch
1. git checkout payment
2. git checkout -b payment

push --> updated code ko github par deployed kar deta h
pull --> updated code github fatched to local system

# Staging Area ->
working directory
staging area
local area


git add .
git commit -m "second commit"
git push
